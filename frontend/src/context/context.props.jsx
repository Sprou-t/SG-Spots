import { createContext } from "react";
// propContext is used for every props that does not need to be drilled
// in the future, need to create more context for more specific props
export const PropsContext = createContext()